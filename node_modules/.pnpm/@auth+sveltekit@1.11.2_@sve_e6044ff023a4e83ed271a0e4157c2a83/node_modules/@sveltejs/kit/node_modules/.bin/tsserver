#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../typescript/bin/tsserver" "$@"
else
  exec node  "$basedir/../../../../typescript/bin/tsserver" "$@"
fi
# cmd-shim-target=C:/Users/jipyt/Documents/GitHub/JipyRaidSimSite/node_modules/.pnpm/@sveltejs+kit@2.68.0_@svelt_3be6e5b378f38b5e3525b9f5a118a478/node_modules/typescript/bin/tsserver
