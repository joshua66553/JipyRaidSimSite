#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../@sveltejs/kit/svelte-kit.js" "$@"
else
  exec node  "$basedir/../../../../@sveltejs/kit/svelte-kit.js" "$@"
fi
# cmd-shim-target=C:/Users/jipyt/Documents/GitHub/JipyRaidSimSite/node_modules/.pnpm/@auth+sveltekit@1.11.2_@sve_e6044ff023a4e83ed271a0e4157c2a83/node_modules/@sveltejs/kit/svelte-kit.js
